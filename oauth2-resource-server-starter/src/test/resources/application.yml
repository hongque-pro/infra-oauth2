infra:
  oauth2:
    client-repository: memory

spring:
  security:
    oauth2:
      authorizationserver:
        issuer: https://testing.com
      resourceserver:
        jwt:
          issuer-uri: https://testing2.com

logging:
  level:
    root: debug
    com.labijie: debug
    org:
      springframework:
        security:
          web: trace