infra:
  caching:
    redis:
      default-serializer: kryo
      regions:
        default:
          url: redis://localhost:6379/1

logging:
  level:
    root: error
    com.labijie: debug

spring:
  security:
    oauth2:

      authorizationserver:
        issuer: https://testing.com