infra:
  caching:
    redis:
      default-serializer: kryo
      regions:
        default:
          url: redis://localhost:6379/1

logging:
  level:
    root: debug
    com.labijie: debug
    org:
      springframework:
        security:
          web: trace

spring:
  security:

    oauth2:
      authorizationserver:
        issuer: https://testing.com
